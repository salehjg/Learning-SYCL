# Example 06: Collective Functions

## Overview 1
```mermaid
mindmap
root(("Collective Functions"))
  id("Workgroup-level")
    id("Barriers")
      id("sycl::group_barrier()")
  id("Subgroup-level")
    id("Barriers")
      id("sycl::group_barrier()")
    id("Group Functions")
      id("sycl::group_broadcast()")
    id("Group Algorithms")
```


## Overview 2
```mermaid
mindmap
root("Group Algorithms")
  id("sycl::joint_any_of()")
  id("sycl::any_of_group()")
  id("sycl::joint_all_of()")
  id("sycl::all_of_group()")
  id("sycl::joint_none_of()")
  id("sycl::none_of_group()")
  id("sycl::shift_group_left()")
  id("sycl::shift_group_right()")
  id("sycl::permute_group_by_xor()")
  id("sycl::select_from_group()")
  id("sycl::joint_reduce()")
  id("sycl::reduce_over_group()")
  id("sycl::joint_exclusive_scan()")
  id("sycl::exclusive_scan_over_group()")
  id("sycl::joint_inclusive_scan()")
  id("sycl::inclusive_scan_over_group()")
```

## Examples
TODO.
